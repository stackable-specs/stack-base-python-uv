# Environment variables for compose.yaml
# Copy to .env and fill in values

# Application
PYTHONUNBUFFERED=1
LOG_LEVEL=${LOG_LEVEL:-INFO}

# Resource limits (override in compose.override.yaml for dev)
APP_CPU_LIMIT=1.0
APP_MEMORY_LIMIT=512M